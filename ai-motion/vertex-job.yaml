# Template only. Replace placeholders at deployment time.
workerPoolSpecs:
  - machineSpec:
      machineType: n1-standard-4
      acceleratorType: NVIDIA_TESLA_T4
      acceleratorCount: 1
    replicaCount: 1
    containerSpec:
      imageUri: REPLACE_WITH_PINNED_TRAINING_IMAGE
      args:
        - --input
        - gs://REPLACE_WITH_DATA_BUCKET/training/current.jsonl
        - --output
        - /gcs/REPLACE_WITH_DATA_BUCKET/models/candidate
